# ~/.tmuxinator/env.yml

name: ma1ca
root: ~/repository/know/inst/curr/ugrad/ma1ca

windows:
  - main:
      layout: even-vertical
      panes:
        - ranger
        - git branch -vva && git status
        - git log --oneline --decorate --graph --all

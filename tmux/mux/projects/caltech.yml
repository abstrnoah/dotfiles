# ~/.tmuxinator/caltech.yml

name: caltech
root: ~/repository/xentity/vocare/caltech

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

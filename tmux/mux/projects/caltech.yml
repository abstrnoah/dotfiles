# ~/.tmuxinator/caltech.yml

name: caltech
root: <%= ENV["ANE_REPOROOT"] %>/xentity/vocare/caltech

windows:
  - main:
      layout: main-horizontal
      panes:
        - ranger
        - tmux select-pane -t 1 && git branch -vva && git status -s

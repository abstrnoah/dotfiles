# ~/.tmuxinator/computation.yml

name: <%= @args[0] %>
root: <%= ENV["ANE_REPOROOT"] %>/know/explore/computation/<%= @args[1] %>

windows:
  - main:
      layout: main-horizontal
      panes:
        - ranger
        - tmux select-pane -t 1 && git branch -vva && git status -s


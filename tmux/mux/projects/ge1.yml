# ~/.tmuxinator/ge1.yml

name: ge1
root: ~/repository/know/inst/curr/ugrad/ge1

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

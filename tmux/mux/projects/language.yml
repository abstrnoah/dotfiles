# ~/.tmuxinator/language.yml

name: language
root: <%= ENV["ANE_REPOROOT"] %>/know/explore/language

windows:
  - main:
      layout: main-horizontal
      panes:
        - ranger
        - tmux select-pane -t 1 && git branch -vva && git status -s

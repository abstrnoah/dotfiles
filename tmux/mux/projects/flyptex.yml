# ~/.tmuxinator/flyptex.yml

name: flyptex
root: ~/repository/know/explore/computation/flyptex

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - ranger
        - git branch -vva && git status -s

# ~/.tmuxinator/library.yml

name: library
root: <%= ENV["ANE_REPOROOT"] %>/know/library/papislib

windows:
  - main:
      layout: main-vertical
      panes:
        - papis git status
  - config:
      root: ~/.env.conf/fs
      panes:
        - vim papis.conf

# ~/.tmuxinator/photo.yml

name: photo
root: ~/repository/recreation

pre_window: cd photo || cd photo.local

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

# ~/.tmuxinator/flypcv.yml

name: flypcv
root: ~/repository/know/explore/computation/flypcv

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

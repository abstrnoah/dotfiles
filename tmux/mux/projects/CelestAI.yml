# ~/.tmuxinator/CelestAI.yml

name: CelestAI
root: ~/repository/know/explore/computation/CelestAI

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

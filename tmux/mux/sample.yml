# ~/.tmuxinator/<%= name %>.yml

name: <%= name %>
root: <%= ENV["ANE_REPOROOT"] %>

windows:
  - main:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status -s

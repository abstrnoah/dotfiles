# ~/.tmuxinator/teach.yml

name: teach
root: ~/repository/xentity/vocare/teach

windows:
  - main:
      layout: even-vertical
      panes:
        - ranger
        - git branch -vva && git status
        - git log --oneline --decorate --graph --all

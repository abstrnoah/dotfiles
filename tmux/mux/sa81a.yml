# ~/.tmuxinator/env.yml

name: sa81a
root: ~/repository/know/inst/curr/ugrad/sa81a

windows:
  - main:
      layout: even-vertical
      panes:
        - ranger
        - git branch -vva && git status
        - git log --oneline --decorate --graph --all

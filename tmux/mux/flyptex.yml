# ~/.tmuxinator/env.yml

name: flyptex
root: ~/repository/know/explore/computation/flyptex

windows:
  - edit:
      layout: main-vertical
      panes:
        - ranger
        - git branch -vva && git status

[ general ]
editor = "/home/abstractednoah/.nix-profile/bin/vim %s"
sort_order = dlct

[ theme ]
columns = title,priority,category,deadline
category_length = 12

row = (help)
row = 1(blank,vpipe,30|blank)
row = 80%(tree,vpipe,30|schedule)
row = (hpipe)
row = (text)
row = (info)

color = white, transparent # Default colors
selected = green, transparent
warn = red, transparent
pipe = white, blue
help = white, blue
